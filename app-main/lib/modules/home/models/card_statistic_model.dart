import 'package:flutter/material.dart';

class CardStatisticModel {
  final int id;
  final String label;
  final String value;
  final Color color;
  final IconData icon ;

  CardStatisticModel(this.id, this.label, this.value, this.color, this.icon);
}
